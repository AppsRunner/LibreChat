services:
  api:
    environment:
      - OPENROUTER_KEY=${OPENROUTER_KEY}   # <-- THIS LINE IS CRITICAL AND WAS MISSING
    volumes:
      - type: bind
        source: ./librechat.yaml
        target: /app/librechat.yaml
